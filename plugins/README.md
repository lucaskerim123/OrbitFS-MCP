# Plugins

This directory is intentionally present as a placeholder only.

The duplicate sorter that used to live under `orbitfs-mcp/plugins` was removed.
The only supported sorter now lives in:

- `F:\OrbitFS Project\orbitfs-panel\plugins\OrbitFS Sorter`
